define(['app'], function (app) {
  app.controller('helpController', [function HelpControllerFactory() {
    // var
    //   help = this,

    //   init = function init() {
    //   };

    // init();
  }]);
});
