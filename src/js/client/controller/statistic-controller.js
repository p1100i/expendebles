define(['app'], function (app) {
  app.controller('statisticController', ['settingService', function statisticControllerFactory(settingService) {
    var
      init = function init() {
      };

    init();
  }]);
});
